# Kick from voice channel

Bot kicking user from voice channel

## Schema
```
{
  "name": "kick example",
  "action": "kickFromVoiceChannel",
  "args": {
    "member": "name#111",
    "channel": "general"
  }
}
```

`action`: kickFromVoiceChannel

`member`: name and tag of the person's discord profile

`channel`: The name of the channel (Optional)