# ActionManager

ActionManager handles all the actions from the payload and calls the appropriate actions

Always go through actionManager whenever an event is created. Do not call the event by itself